dbo_crown_noble_marry = {
	sound = UI_action_character_marry_noble
	is_consort_action = yes
	on_own_nation = yes
	context_menu_click_mode = click
	message = no
	ai_tick = monthly
	ai_tick_frequency = 1
	potential = {
		scope:actor = {
			has_or_had_tag = TUR
		}
	}
	select_trigger = {
		looking_for_a = character
		source = actor
		target_flag = recipient
		name = "choose_character"
		column = {
			data = name
		}
		visible = {
			is_alive = yes
			is_adult = yes
			is_female = no
			OR = {
				has_estate = estate_type:crown_estate
				has_estate = estate_type:nobles_estate
			}
		}
		enabled = {
			character_age < 40
			total_abilities > 120
			has_available_marriage_slot = yes
			modifier:blocked_from_marriage = no
		}
	}
	ai_will_do = {
		add = {
			desc = "BASE"
			value = 100
		}
	}
	effect = {
		if = {
			limit = {
				exists = scope:actor
			}
			scope:actor = {
				dbo_create_and_marry_lady = {
					married_character = scope:recipient
				}
			}
		}
	}
}
