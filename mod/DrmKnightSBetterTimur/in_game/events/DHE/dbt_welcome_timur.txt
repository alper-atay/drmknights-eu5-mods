namespace = dbt_welcome_timur
dbt_welcome_timur.1 = {
	type = country_event
	title = dbt_welcome_timur.1.title
	desc = dbt_welcome_timur.1.desc
	historical_info = dbt_welcome_timur.1.historical_info
	fire_only_once = yes
	dynamic_historical_event = {
		tag = TIM
		from = 1337.1.1
		to = 1837.1.1
		monthly_chance = 100
	}
	trigger = {
		country_exists = c:TIM
	}
	option = {
		name = dbt_welcome_timur.1.a
		set_variable = {
			name = dbt_timur_the_genius
			value = yes
		}
		set_variable = {
			name = dbt_war_wolf
			value = yes
		}
		set_variable = {
			name = dbt_lessons_from_mongols
			value = yes
		}
		historical_option = yes
		if = {
			limit = {
				NOT = {
					has_reform = government_reform:dbt_timur_the_genius
				}
			}
			add_reform = government_reform:dbt_timur_the_genius
		}
		if = {
			limit = {
				NOT = {
					has_reform = government_reform:dbt_war_wolf
				}
			}
			add_reform = government_reform:dbt_war_wolf
		}
		if = {
			limit = {
				NOT = {
					has_reform = government_reform:dbt_lessons_from_mongols
				}
			}
			add_reform = government_reform:dbt_lessons_from_mongols
		}
		add_stability = stability_extreme_bonus
	}
}
