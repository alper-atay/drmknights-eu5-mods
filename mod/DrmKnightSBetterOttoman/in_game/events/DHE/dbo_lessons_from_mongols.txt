namespace = dbo_lessons_from_mongols
dbo_lessons_from_mongols.1 = {
	type = country_event
	title = dbo_lessons_from_mongols.1.title
	desc = dbo_lessons_from_mongols.1.desc
	historical_info = dbo_lessons_from_mongols.1.historical_info
	fire_only_once = yes
	dynamic_historical_event = {
		tag = TUR
		from = 1337.1.1
		to = 1837.1.1
		monthly_chance = 25
	}
	trigger = {
		OR = {
			current_age_or_later = {
				age = {
					age = age_3_discovery
				}
			}
			OR = {
				has_advance = timariots
				has_advance = unlock_cavalry_advance
			}
		}
		NOT = {
			has_reform = government_reform:dbo_lessons_from_mongols
		}
	}
	option = {
		name = dbo_lessons_from_mongols.1.a
		set_variable = {
			name = dbo_lessons_from_mongols
			value = yes
		}
		historical_option = yes
		add_reform = government_reform:dbo_lessons_from_mongols
		add_stability = stability_severe_bonus
	}
}
