namespace = dbo_dhe
dbo_dhe.49 = {
	type = country_event
	title = dbo_dhe.1.title
	desc = dbo_dhe.1.desc
	fire_only_once = yes
	dynamic_historical_event = {
		tag = TUR
		from = 1337.1.1
		to = 1837.1.1
		monthly_chance = 15
	}
	trigger = {
		has_slavery = yes
		has_advance = recruitment_law_advance
		OR = {
			has_policy = ottoman_war_slave_tax
			has_policy = devshirme_system_policy
		}
		NOT = {
			has_variable = devshirme_abolished
		}
	}
	option = {
		name = dbo_dhe.a
		historical_option = yes
		add_policy = policy:ottoman_war_slave_tax
		add_estate_satisfaction = {
			type = estate_type:nobles_estate
			value = estate_satisfaction_extreme_penalty
		}
	}
}
